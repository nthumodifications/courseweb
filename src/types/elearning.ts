export type ElearningObject = {
    announcements: {
        courseId: number,
        courseName: string,
        date: string,
        title: string,
        announcer: string,
        url: string,
    }[]
}