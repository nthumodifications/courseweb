'use client';
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"
import { useState, useEffect } from 'react'

const Shops = ({ data }) => {

  return (<div>
    
    {/*{data.map(([area, image, name, note, phone, schedule], index) => 
      <div key={name || index} className="p-8">
        <Card>
          <CardHeader>
            <CardTitle>{name}</CardTitle>
            <CardDescription>{area}</CardDescription>
          </CardHeader>
          <CardContent>
            <p>Card Content</p>
          </CardContent>
          <CardFooter>
            <p>Card Footer</p>
          </CardFooter>
        </Card>
      </div>
    )}*/}
  </div>)
}

export default Shops