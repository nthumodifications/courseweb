{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["db:generate"],
      "outputs": ["dist/**"],
      "env": ["DATABASE_URL", "DIRECT_URL"]
    },
    "db:generate": {
      "cache": false,
      "outputs": ["node_modules/.prisma/**"],
      "env": ["DATABASE_URL", "DIRECT_URL"]
    },
    "dev": {
      "dependsOn": ["db:generate"],
      "cache": false,
      "persistent": true
    },
    "start": {
      "dependsOn": ["db:generate"],
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["db:generate"]
    },
    "preproduction": {
      "dependsOn": ["security:check", "test", "build"]
    },
    "security:check": {
      "dependsOn": ["test:security", "security:scan"]
    }
  }
}
